---
const { backTo } = Astro.props;
---

{
  backTo ? (
    <a
      href={`/${backTo}`}
      class="text-purple-dark dark:text-purple-light italic"
    >
      ← Back to {backTo}
    </a>
  ) : (
    <a href="/" class="text-purple-dark dark:text-purple-light">
      ← Back home
    </a>
  )
}
